// Server build version and the storage schema version.
// SCHEMA_VERSION tracks the SQLite PRAGMA user_version that the migrations
// advance to. Bump it whenever a new migration is added.
export const SERVER_VERSION = "0.1.0";
export const SCHEMA_VERSION = 1;
